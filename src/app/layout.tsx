import type { Metadata } from "next";
import { Geist, Geist_Mono, Space_Grotesk } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

const spaceGrotesk = Space_Grotesk({
  variable: "--font-space-grotesk",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Taras Olefirenko | Desarrollador FullStack + IA",
  description:
    "Portfolio de Taras Olefirenko, desarrollador FullStack con foco en producto e IA aplicada.",
  metadataBase: new URL("https://taras-olefirenko.dev"),
  openGraph: {
    title: "Taras Olefirenko | Desarrollador FullStack + IA",
    description:
      "Productos reales, software útil e IA aplicada donde aporta valor.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} ${spaceGrotesk.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col bg-background text-foreground">
        {children}
      </body>
    </html>
  );
}
